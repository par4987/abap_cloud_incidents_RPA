projection;
strict ( 2 );
use draft;

define behavior for ZC_DT_INCT_1237
{
  use create;
  use update;
  use delete;

  use action changeStatus;

  use action Activate;
  use action Discard;
  use action Edit;
  use action Resume;
  use action Prepare;

  use association _History { create; with draft; }
}

define behavior for ZC_DT_INCT_H_1237
{
  use update;
  use delete;

  use association _Incident { with draft; }
}